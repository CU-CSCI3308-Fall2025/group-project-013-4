version: "3.9"

services:
  web:
    image: nginx:latest
    container_name: finance_tracker_web
    ports:
      - "8080:80"
    volumes:
      - ./frontend:/usr/share/nginx/html
    restart: always
